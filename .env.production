# Loaded automatically by `vite build` (production mode).
# The API + sockets + uploads are all served behind Nginx under the same
# /OrderNow sub-path, so this is a same-origin, relative-to-domain URL.
VITE_API_URL=https://nexussoftlab.com/OrderNow/api
